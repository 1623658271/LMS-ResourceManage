# MEMORY.md - 长期记忆

## 项目：立杰人力资源管理系统

- **项目路径**：`e:\PostGraduate\ProgrammingStudy\AutoScanMind\Li---Jie\li_jie_hr\`
- **技术栈**：Python 3 + pywebview 5.x + HTML/CSS/JS（纯本地 Chromium 窗口，无网络依赖）
- **运行**：`pip install webview>=5.0.0 && python main.py`，或双击 `run.bat`
- **打包**：`pyinstaller build.spec`（待添加）
- **状态**：2026-04-11 完成 pywebview + FastAPI v2 版本全部功能，语法检查通过，数据验证通过
- **数据库**：新增 orders 表（含 year/month/remark）和 order_models 关联表，work_records 加 order_id FK，支持 v1→v2 迁移
- **已知 bug 修复**：型号单价表 API 返回的 prices 字典 key 从 tuple 改为字符串 `"model_id,sub_dept_id"`，避免 FastAPI jsonable_encoder 的 unhashable list 错误
- **种子数据**：`seed_data.py` 生成 14 名员工、6 个型号、3 个订单（ORD-001~003）、97 条做货记录（2026-04）
- **用户需求（v2）**：资源总览（首页导航）+ 订单管理（CRUD+型号联动）+ 成员/部门/型号单价/做货编辑/总工资表/快捷计算，共 8 个功能页
- **做货编辑**：选订单 → 选型号 → 添加行，订单/型号列为下拉框（联动），Tab 在员工格之间跳转；新增"切换工资视角"按钮（只读显示每个格子的对数×单价）；保存用 order_id
- **快捷计算**：按大部门分组多表格（v3.0），每个大部门独立表格；表头=各小部门单价|各成员对数|行合计|操作；单价和对数都纯手工输入（无型号）；`_qcDeptRows = { "deptId_rowIdx": { subDeptId: 单价 } }`，`qtyData = { "rowKey,empId": 对数 }`；移除了型号、单价编辑区域和模板功能
- **总工资表数据源切换**：资源总览右上角开关（localStorage `useQcSalary`），启用后总工资表读快捷计算数据（`source=qc`），关闭则读做货编辑数据；做货编辑和快捷计算的工资分别计算互不影响
- **成员管理**：紧凑卡片（姓名/性别♀♂/大部门/小部门 + 详情/编辑/增扣/删除按钮）；点击「详情」跳转到成员详情页
- **成员详情**：GET /api/employees/{id}/work-history，返回员工入职以来所有月份的做货记录（按年月倒序）；顶部汇总卡片显示累计做货对数/做货工资/人工增扣/总收入；每月一个折叠块，含订单号/型号/对数/单价/工资明细，人工增扣行高亮显示
- **配色规范**：成员姓名=蓝色(#2563eb)，大部门=橙色(#d97706)，小部门=绿色(#059669)；所有页面统一应用
- **表格自适应显示**：做货编辑和快捷计算的员工列（>8人时）自动拆分为多个独立表格纵向排列，每表含完整固定列（订单号/型号/行合计/操作）；大额工资使用 fmtCompact 紧凑格式（<1万显示¥1234.56，≥1万显示¥1.2万，≥100万显示¥100.0万），鼠标悬停显示完整数值；紧凑模式下自动缩小字号（11px/10px）；分组标签（"第X组/共Y组"）分隔各表格
- **系统设置页面**：左侧导航（外观/字体/表格/颜色主题/布局/数据），支持深色模式、圆角/阴影风格、字体大小（基础/标题/大标题）、字体族、表格字号/行高/斑马纹/紧凑模式/分组数量、5种颜色预设（蓝色/绿色/紫色/橙色/红色）+自定义颜色、侧边栏宽度/内容边距/卡片间距、导出/导入/重置设置；设置存储在 localStorage，自动应用
- **v2.1 更新（2026-04-12）**：
  1. ~~成员姓名颜色改为黑色~~ → 修复：新增 `.member-list-name-color` 类，仅成员管理页姓名为黑色
  2. 批量添加成员改为大表格弹窗（显示大部门/小部门，可多行输入，底部有"继续添加行"按钮）
  3. ~~做货编辑改为可直接添加行~~ → 修复：移除 `addWorkRow` 的订单检查
- **v2.1.1（2026-04-12）**：修复颜色影响其他页问题，修复做货编辑添加行问题
- **v2.1.2（2026-04-12）**：修复addWorkRow为空行初始化所有员工格子；做货编辑/快捷计算表格改为浅白风格；员工姓名改为黑色
- **v2.1.3（2026-04-12）**：修复loadSettings()从未被调用的问题，设置可正确持久化；做货编辑表格添加删除行按钮
- **v2.1.4（2026-04-12）**：修复deleteWorkRow按钮onclick参数（添加引号）；固定默认显示模式为单表格；隐藏设置中的表格显示方式选项
- **v2.1.5（2026-04-12）**：做货编辑和快捷计算的表格文字（型号/员工输入格/行合计）和删除按钮都设为居中显示
- **v2.1.6（2026-04-12）**：修复做货编辑添加行逻辑（虚拟行ID）；表格UI更新（订单/型号浅绿背景，成员浅绿背景+深绿文字）；自动保存功能（切换工资视角/页面时）
- **v2.1.7（2026-04-12）**：修复做货编辑删除行功能（添加_deletedRowKeys集合跟踪已删除行，防止重新加载时出现）
- **v2.1.8（2026-04-12）**：修复做货编辑添加行后选择订单会多出行的问题；虚拟行选择订单时只更新下拉选项不重新渲染；新行显示在最前面；操作列（删除按钮）移到最左边
- **v2.1.9（2026-04-13）**：彻底修复做货编辑：删除改为真正的数据库删除（新增/api/work-row和crud.delete_work_row）；添加行流程简化（虚拟行有data-temp-id，选择型号后自动转为实际行）；autoSaveWorkRecords修复（只保存有实际数值的记录）
- **v2.1.10（2026-04-13）**：彻底修复虚拟行转换问题（初始化改为null，立即保存quantity=0标记行存在，重新加载records后再渲染）
- **v2.1.11（2026-04-13）**：简化做货编辑逻辑：不需要虚拟行机制，新建行就是实体行，没选型号就不算工资直接跳过
- **v2.1.12（2026-04-13）**：修复：不要禁用型号下拉，只是不选型号时不计算那一行的工资
- **v2.1.13（2026-04-13）**：彻底修复做货编辑：loadWorkRecords时重置tempWorkRows和_tempRowCounter；addWorkRow直接初始化所有员工单元格；deleteTempRow同时清除单元格数据
- **v2.1.14（2026-04-13）**：彻底修复添加行变多行问题：构建rows时跳过orderId=0或modelId=0的无效行
- **v2.1.15（2026-04-13）**：修复订单和型号下拉selected属性：保存后重新渲染时正确显示已选择的订单和型号
- **v2.1.16（2026-04-13）**：修复后端save_work_record逻辑：qty <= 0改为qty < 0才删除，这样quantity=0能正确保存到数据库，临时行才能转换为实际行
- **JS模块化重构（2026-04-12）**：将 `index.html` 从 ~4000行单体拆分为 13 个独立 `.js` 模块（state/utils/api/nav/member/dept/order/price/work-edit/salary-summary/quick-calc/settings/init），所有 13 个文件均已完整实现并通过语法检查；`initSettingsPage` 从 quick-calc.js 移至 settings.js；添加 `_savedDarkColors` 声明修复 settings.js 深色模式切换 bug
- **v2.2.1（2026-04-12）**：做货编辑大改
  1. 移除"保存全部"按钮，改为纯自动保存（300ms防抖）
  2. 修复autoSaveWorkRecords：正确处理0值（val>0保存，val<=0删除），不在循环内delete
  3. 修复deleteWorkRow/deleteTempRow：正确清除_dirtyCells防止多出行
  4. 取消订单→型号强依赖：型号下拉显示所有型号
  5. 移除头部订单/型号选择器，简化工具栏
  6. 从_dirtyCells构建行的逻辑移除，防止重复渲染多出行
  7. 修复key格式不一致：onOrderCellChange和deleteTempRow使用逗号分隔符，改为管道符，与其他地方保持一致
- **v2.2.2（2026-04-12）**：照搬快捷计算结构重写做货编辑
  1. 行索引模式：`_workRowSelects = {rowIdx: {orderId, modelId}}`
  2. 简化状态：删除 tempRows/_dirtyCells/_deletedRowKeys
  3. 统一 key 格式：`orderId,modelId,empId`（逗号分隔）
  4. autoSaveWorkRecords：300ms防抖，与快捷计算一致
  5. Tab 导航、添加/删除行、工资视角切换全部照搬快捷计算
