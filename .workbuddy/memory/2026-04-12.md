# 2026-04-12 日志

## 完成的任务

### 三个需求实现 (v2.1)

1. **成员姓名颜色改为黑色**
   - 修改 `.member-name-color` CSS，从浅蓝色 `#60d9fa` 改为黑色 `#000`
   - 不影响其他页面的颜色显示

2. **批量添加成员改为大表格弹窗**
   - 重写 `showBatchAddMemberModal()` 函数
   - 表格显示：序号/姓名/性别/大部门/小部门/删除按钮
   - 切换大部门时自动联动更新小部门选项
   - 支持"添加行"和"继续添加行"按钮动态添加新行
   - 底部"批量添加"按钮批量提交所有有效行

3. **做货编辑改为直接添加行**
   - 修改空状态判断，只要有员工就显示表格（不强制要求订单）
   - 修改 `addWorkRow()` 函数，无需先选订单/型号即可添加行
   - 新增 `onOrderCellChange()` 函数处理行内订单-型号联动
   - 切换订单时自动更新该行型号下拉选项
   - 保存时自动跳过没有选择型号的行

## Git 版本

- v2.0: 完整功能基线
- v2.1: 三个需求完成
- v2.1.1: 修复颜色影响和添加行问题
- v2.1.2: 修复addWorkRow逻辑 + 浅白风格 + 黑色姓名
- v2.1.3: 修复设置保存 + 删除行按钮
- v2.1.4: 固定单表格显示
- v2.1.5: 表格文字和删除控件居中
- v2.1.6: 添加行逻辑 + UI配色 + 自动保存
- v2.1.7: 修复删除行功能
- v2.1.8: 修复添加行后选择订单会多出行 + 操作列移最左边

## v2.1.8 修复详情

### 问题修复
1. **添加行后选择订单会多出行**：虚拟行选择订单时只更新下拉选项，不触发完整重新渲染
2. **calcRowTotal处理null值**：虚拟行（未选订单/型号）不计算行合计

### UI更新
1. **操作列移到最左边**：删除按钮在第一列，方便操作
2. **新行显示在最前面**：rows数组中虚拟行在前面，渲染时新行显示在表格顶部
